 // 1. This is a parseInt situation
function convertToInteger(str) {
    // what does this is takes the str parameter
    // and parses it using parseInt
    // then returns the number
return parseInt(str)
}
console.log(convertToInteger("56"))